# Example environment variables for production deployment
# Copy this file to .env and update with your values

# Database password (used by PostgreSQL container)
DB_PASSWORD=changeme_to_secure_password

# Optional: Rust log level
# RUST_LOG=info

# Optional: Override the APP_ENV (defaults to prod in production)
# APP_ENV=prod
